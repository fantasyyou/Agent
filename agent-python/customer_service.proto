syntax = "proto3";

package customer;

import "google/protobuf/struct.proto";

// 请求和响应字段由 contracts/dialogue-turn.schema.json 约束，Struct 负责跨语言传输。
service CustomerService {
  rpc Answer(google.protobuf.Struct) returns (google.protobuf.Struct);
}
